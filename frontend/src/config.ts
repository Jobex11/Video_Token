/*
import {} from "@solana/wallet-adapter-wallets";

import {} from "@reown/appkit-adapter-solana/react";
import {} from "@reown/appkit/networks";

import {} from "@reown/appkit/react";

export const projectId = import.meta.VITE_PROJECT_ID || "";

const networks = [solanaDevnet];

const solanaAdapter = new SolanaAdapter({
  wallets: [
    // enter wallets here ...
  ],
});


*/
